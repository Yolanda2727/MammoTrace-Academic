# Optional experimental ML profile. Not required for the public no-weights UI.
-r requirements.txt
# CPU index avoids downloading CUDA wheels on non-Darwin systems.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0+cpu; platform_system != "Darwin"
torch==2.10.0; platform_system == "Darwin"
keras==3.13.2
h5py==3.15.1
